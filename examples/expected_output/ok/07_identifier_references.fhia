-------------------------------------------------
let x: i64 = (42)
let y: i64 = (x: ?)
let main: i32 = ((y: ? as i32))
-------------------------------------------------
let x: i64 = (42)
let y: i64 = (x: i64)
let main: i32 = ((y: i64 as i32))
-------------------------------------------------
; ModuleID = 'main'
source_filename = "main"

@x = constant i64 42
@y = constant i64 42

define i32 @main() {
entry:
  %y = load i64, ptr @y, align 8
  %trunc = trunc i64 %y to i32
  ret i32 %trunc
}
Return code: 42
