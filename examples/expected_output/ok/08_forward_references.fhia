-------------------------------------------------
let x: i64 = (y: ?)
let y: i64 = (42)
let main: i32 = (i32 (x: ?))
-------------------------------------------------
let x: i64 = (y: i64)
let y: i64 = (42)
let main: i32 = (i32 (x: i64))
-------------------------------------------------
; ModuleID = 'main'
source_filename = "main"

@x = global i64 0
@y = global i64 0

define i32 @main() {
entry:
  store i64 42, ptr @y, align 8
  %y = load i64, ptr @y, align 8
  store i64 %y, ptr @x, align 8
  %x = load i64, ptr @x, align 8
  %trunc = trunc i64 %x to i32
  ret i32 %trunc
}
Return code: 42
