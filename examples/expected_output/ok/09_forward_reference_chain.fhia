-------------------------------------------------
let x: i64 = (z: ?)
let y: i64 = (x: ?)
let z: i64 = (42)
let main: i32 = (i32 (y: ?))
-------------------------------------------------
let x: i64 = (z: i64)
let y: i64 = (x: i64)
let z: i64 = (42)
let main: i32 = (i32 (y: i64))
-------------------------------------------------
; ModuleID = 'main'
source_filename = "main"

@x = constant i64 42
@y = constant i64 42
@z = constant i64 42

define i32 @main() {
entry:
  %y = load i64, ptr @y, align 8
  %trunc = trunc i64 %y to i32
  ret i32 %trunc
}
Return code: 42
